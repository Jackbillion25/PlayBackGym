---
// Este subdominio (app.playbackgym.fitness) es SOLO la app: no hay landing aquí.
// La landing de marketing vive aparte en la raíz del dominio (playbackgym.fitness).
// Entrar a "/" = ir directo al login (o al panel si ya hay sesión).
if (Astro.locals.user) return Astro.redirect('/app')
return Astro.redirect('/login')
---
